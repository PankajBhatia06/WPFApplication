<Page x:Class="VVDNApplicationWPF.Views.CategoryListView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:VVDNApplicationWPF.Views" xmlns:models="clr-namespace:VVDNApplicationWPF.Models" d:DataContext="{d:DesignInstance Type=models:Brand}"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="CategoryListView">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Button Grid.Row="0" x:Name="Add_Category" Content="Add New Category" Width="110" HorizontalAlignment="Left"/>

        <StackPanel Grid.Row="1">
            <DataGrid ItemsSource="{Binding ListCategories}" IsReadOnly="True" AutoGenerateColumns="False">
                <DataGrid.Columns>
                <DataGridTextColumn Header="Category Id" Width="100" Binding="{Binding Id}"/>
                    <DataGridTextColumn Header="Category Name" Width="*" Binding="{Binding Name}"/>
                </DataGrid.Columns>
        </DataGrid>
        </StackPanel>
    </Grid>
</Page>
